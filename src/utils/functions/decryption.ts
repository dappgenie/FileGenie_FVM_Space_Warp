// import * as rsa from 'node-rsa';

// // read in the encrypted file
// const encryptedData = fs.readFileSync("file.txt.encrypted");

// // Create a new RSA key instance
// const key = new rsa();

// // import the private key
// key.importKey(privateKey,'pkcs1-private-pem');

// // decrypt the file using the private key
// const decryptedData = key.decrypt(encryptedData).toString();

// // write the decrypted data to a new file
// fs.writeFileSync("file.txt", decryptedData);
